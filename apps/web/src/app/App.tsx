/**
 * Placeholder shell — replaced by the router + providers in step 3.
 */
export function App() {
  return (
    <main className="paper-grain relative grid min-h-dvh place-items-center px-6">
      <div className="max-w-md text-center">
        <p className="label mb-4">CART Paper</p>
        <h1 className="font-display text-4xl font-semibold text-ink-text">A private library, at night.</h1>
        <p className="mt-4 text-sm leading-relaxed text-ink-muted">
          Scaffold is up. The reader, the editor and the passage-anchored discussions land in the
          steps that follow.
        </p>
      </div>
    </main>
  )
}
